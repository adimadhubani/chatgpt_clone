{
    "name": "chatgpt_clone",
    "version": "1.0.0",
    "scripts": {
        "dev": "concurrently \"cd server && nodemon index.js\" \"cd client && vite dev\"",
        "start": "cd server && node index.js"
    },
    "devDependencies": {
        "concurrently": "^9.2.0"
    }
}
